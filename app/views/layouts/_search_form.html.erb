<div class="container-fluid pull-right">
    <div class="form-inline">
        <%= search_form_for @q do |f| %>
        <%= f.text_field search, class: "form-control", placeholder: "Buscar", onclick: 'this.select();' %>
        <%= f.submit "Buscar", class: "btn btn-default" %>
        <% end %>
    </div>
</div>
